# G3 Energy — environment variables
# Copy to `.env.local` and fill in for production.

# Optional: recipient for contact form notifications
CONTACT_TO_EMAIL=info@g3energy.com

# Optional: rate-limit window (requests per IP per minute)
CONTACT_RATE_LIMIT=5
